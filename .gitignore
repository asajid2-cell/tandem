# dependencies
node_modules/

# local runtime state — PRIVATE (session ids, transcripts, verdicts, daemon pid)
.state/

# local machine config (copy tandem.config.example.json → tandem.config.json)
tandem.config.json

# misc
*.log
.DS_Store
sessions/
tandems/
