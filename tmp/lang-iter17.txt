FEATURE — the optional AI helper (bring-your-own API key, per account). You are the Lead; foreground-supervised. Working dir z:\328\CMPUT328-A2\codexworks\301\language. Accounts are live: backend server/ has per-account key storage (server/keyStore.js, /api/keys) gated by hl-auth session (server/auth.js requireSession). The frontend has #/ja/settings where a signed-in user saves a provider key {provider, apiKey, baseUrl?, model?} (providers: DeepSeek / OpenAI-compatible / Other).

BUILD an optional AI study helper that uses the user's OWN stored key (never exposed to the browser):
1. BACKEND: server/ai.js + route POST /api/ai (gated by requireSession):
   - body: { prompt: string, context?: {kind, lang, item} }  (context = optional: the current word/kanji/scene the learner is on).
   - load THIS user's stored key via keyStore. If none set → 400 {error:"no_key"}.
   - call the provider via the OpenAI-compatible Chat Completions API at the key's baseUrl (DeepSeek default https://api.deepseek.com, model default deepseek-chat; OpenAI-compatible/Other use the stored baseUrl+model). Send a concise SYSTEM prompt: a focused Japanese/French/Korean LEARNING helper — explain the asked word/grammar simply, give 1-2 natural example sentences, keep it short; if context is provided, tailor to that item and the learner's beginner level. Return { reply: string } (non-streaming is fine). Reasonable max_tokens + timeout. Never return the key. Clear errors: no_key, provider_error (with a safe message, not the raw key).
   - This is the ONE intended outbound call (server-side, user's key) — the app is otherwise self-contained; that's fine.
2. FRONTEND: an "Ask AI" helper, visible ONLY when signed in AND a key is set (else a gentle hint linking to settings). A small panel/affordance on the learn + scene + kanji views (and/or a dedicated route) where the learner types a question (optionally pre-filled with the current item as context) → POST /language/api/ai via ${import.meta.env.BASE_URL}api/ai (credentials include) → render the reply. On-brand calm design, light/dark, hidden-English behavior unaffected. Loading + error states (esp. no_key → "Add your API key in Settings"). Keep it OPTIONAL and unobtrusive.
3. Make sure signed-out / no-key users see the app exactly as before (no breakage, no AI clutter).

VERIFY: npm run build green, npm run lint:content 0 leaks, npm test green (0 skipped; add a small backend test for /api/ai no_key path if easy). Run the ui-craft battery on the AI states (helper hint when no key, ask panel, loading, reply, error) light+dark → save .uicraft-shots/iter17-final + give path. Do NOT git commit, do NOT deploy. REPORT: backend route + provider-call shape + safety (key never leaked), frontend UX + where it surfaces, battery path, exact build/lint/test output. Keep it tight.
