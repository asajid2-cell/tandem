FRONTEND chunk — INLINE hl-auth login/account UI + per-account API-key settings (no redirect). You are the Lead; foreground-supervised. Working dir z:\328\CMPUT328-A2\codexworks\301\language. The backend (server/) is built & committed: GET /api/me {authenticated,user}, GET/PUT/DELETE /api/keys (masked, gated). hl-auth same-origin JSON: POST /auth/api/login {username,password}, GET /auth/api/me, POST /auth/api/logout (cookie hl_session). The app stays PUBLIC; login is OPTIONAL and unlocks key storage (and later AI).

BUILD (frontend only; keep the calm reading-first design + light/dark + hidden-English behavior):
1. Account state: on load, fetch /api/me (credentials:'include'). Header shows either a subtle "Sign in" button (signed out) or "<username>" + a small account menu (signed in) with Sign out (POST /auth/api/logout then refresh).
2. INLINE sign-in (NOT a redirect): clicking "Sign in" opens an inline panel/modal with username + password → fetch POST /auth/api/login (credentials:'include', JSON). hl-auth uses a double-submit CSRF cookie — if a CSRF cookie is present (e.g. hl_csrf, httpOnly:false), read it and send it (header x-csrf-token or _csrf field) so login isn't rejected; handle both success and error (bad creds) inline with a clear message. On success: refresh /api/me, close panel. Include a small "Need an account? Create one" link to /auth/claim (invite-based signup stays on hl-auth) — but the LOGIN itself is fully inline.
3. API-KEY SETTINGS (only visible when signed in): a settings panel/route where the user can add an AI provider key — fields: provider (select: DeepSeek / OpenAI-compatible / other), API key (password field), optional base URL + model. Save → PUT /api/keys; show existing keys MASKED (provider + last4 + "set"); allow delete (DELETE /api/keys/:provider). Explain briefly: "Your key is stored on your account and used only for the optional AI helper." This sets up the AI feature next.
4. DEV PROXY: add a Vite dev-server proxy so /api → http://127.0.0.1:4310 (the local backend) for local testing. /auth requires the live hl-auth (not local) — that path is verified after deploy; degrade gracefully in dev (if /auth/api/me 404s, treat as signed out, no console spew).
5. Wire it cleanly into the existing nav/shell; nothing should break for signed-out users.

VERIFY: npm run build green, npm run lint:content 0 leaks, npm test green (0 skipped). Run the ui-craft battery on the new states (signed-out header, sign-in panel, signed-in header, key-settings empty, key-settings with a masked key) in light AND dark → save to .uicraft-shots/iter16b-final, give me the path. Do NOT git commit, do NOT deploy.
REPORT: components/routes added, how login stays inline + CSRF handling, the key-settings UX, battery path, exact build/lint/test output. Keep it tight.
