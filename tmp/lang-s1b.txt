FEATURE: Structured Lessons — Loop S1b (UI: roadmap + lesson player + routing + make lessons PRIMARY). Foreground; resume. Working dir = language repo. Read docs/lessons-design.md. S1a is done: types Lesson/Chapter/LessonStep, ContentBundle.chapters?/lessons?, progress.completedLessonIds + markLessonComplete(lesson), and content/ja/lessons.ts (jaChapters + jaLessons, 1 chapter 'ja-ch-1', 1 lesson 'ja-lesson-1' with steps intro→vocab→grammar→grammar→scene→reading→check). Build is green. Reuse existing components for visual consistency; respect readingHints + hidden-English + dark mode (inherited from reused components).

In src/App.tsx:
1) View union: add 'path' (roadmap) and 'lesson' (player). toView() accepts them. Route parsing: '/ja/path' -> view 'path'; '/ja/lesson/<id>' -> view 'lesson' carrying the lesson id (reuse the route.sceneId slot for the id, like 'scene'/'passage').
2) Render branches (the big ternary): route.view==='path' -> <RoadmapView bundle progress.../>; route.view==='lesson' -> <LessonPlayer bundle lessonId={route.sceneId} query/>.
3) RoadmapView: render bundle.chapters (sorted by order) as sections (title + subtitle), each lesson (from bundle.lessons by chapter.lessonIds) as a NODE in a vertical path. Node state: DONE if id in progress.completedLessonIds; CURRENT = first not-done lesson in order; LOCKED = lessons after the current (gate: a lesson is open if it's the first lesson OR the previous lesson by global order is complete). Show lesson title + objective + state badge + a progress summary (e.g. 'X of Y lessons'). Clicking an open node -> navigateTo('/ja/lesson/<id>'). Use ui-craft-consistent cards (match Scene/Reading hub styling), >=44px targets, no overflow, dark-mode tokens.
4) LessonPlayer: find lesson by id (fallback to first lesson). Walk steps with a step index via ?step=N (resume/back/next). Render a top PROGRESS BAR (step N of M). Render each step by kind, REUSING existing rendering:
   - intro: a clean objective card (lesson.title + objective + 'Start').
   - vocab: render the step.lexOrders words using the SAME word-card layout NewWordFlow uses (headword, readingHints ruby/kana/romaji/off, reveal-meaning, audio) — one card per word in a short sequence OR a vertical stack; 'continue' advances.
   - grammar: render step.grammarOrder pattern with the SAME grammar-card layout (title/formula/explanation/example with furigana+audio).
   - scene: embed the existing SceneReader for step.sceneId. reading: embed PassageReader for step.readingId.
   - check: render step.quizItems with the SAME quiz UI checkpoints/scenes use; on pass, advance to completion.
   Final step done -> a COMPLETION screen ('Lesson complete') that calls progress.markLessonComplete(lesson) ONCE and offers 'Back to path' (-> /ja/path) and 'Next lesson' if one is unlocked. Don't double-fire markLessonComplete on re-render.
5) Make lessons PRIMARY: when a JA learner lands (LanguagePicker selects ja, and the default JA route), send them to '/ja/path' instead of '/ja/learn'. Add a prominent header nav item 'Lessons' (-> /ja/path). Keep Learn/Scenes/Review/Grammar/Kanji reachable but grouped/labeled as 'Practice' (a simple grouping or a 'Practice' submenu/section is fine — don't remove them).

VERIFY (iterate to green, EXIT CODES): `npx tsc -b` exit 0 AND `npm run build` exit 0; `npm test` pass. Do NOT edit vite.config.ts, do NOT run vite preview (locks dist), audio, deploy, or commit. Report: tsc rc, build rc, the new component names, the nav change, and how lesson completion is guarded against double-fire.
