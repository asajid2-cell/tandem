ITERATION 11 (you are the Lead; same Foothold project; iter1-10 committed & LIVE. Japanese: 2 units + checkpoints + audio. French: 1 unit + checkpoint + audio. The architecture is proven language-agnostic. Now deliver the THIRD brief language: KOREAN.)

Watcher committed + redeployed iter10 (French verified live). Continue.

SCOPE — Korean Unit 1 + the Korean tokenizer (this resolves plan section 20#2):
1. KO TOKENIZER (engine/lint.ts, a new TokenizerAdapter for 'ko'): implement OPTION B — a curated longest-match tokenizer scoped to the CLOSED lexicon. Korean writes spaces between eojeol (phrase-words) but particles attach to nouns inside an eojeol (e.g. 저는 = 저 + 는, 물을 = 물 + 을). Approach: split on whitespace/punctuation into eojeol, then within each eojeol do longest-match segmentation against the KNOWN Korean lexicon entries (content words AND particles 은/는/이/가/을/를/에/에서/도/와/과/의 etc. as first-class LexEntrys), emitting those tokens; inflections handled via the same grammar-license model as JA (classed tokens like ~입니다 / ~ㅂ니다 licensed by a grammar pattern). Because the universe is closed, longest-match is tractable and EXACT. Keep it fail-closed (unknown segment that can't be matched = surface it as a leak token, never silently drop). Wire createKoreanTokenizer into scripts/lint-content.ts.
2. KO TOKENIZER TESTS: add fixtures — a clean KO sentence with particles + a licensed verb/copula inflection that PASSES; a planted KO leak (unintroduced content word) that MUST be caught; an inflection caught when its grammar license isn't known. (Mirror the JA fixtures.)
3. AUTHOR KO Unit 1 in content/ko/seed.ts: hangul primer (the syllable blocks/jamo needed to read this unit), ~16-18 genuinely high-frequency words (저/나 I, particles 은/는/이/가/을/를, 네/아니요 yes/no, 물 water, 학생 student, 사람 person, 있다/없다 exist, 가다/오다 go/come, 이것/그것 this/that, etc. — your judgment), 1-2 grammar patterns (polite copula 입니다/이에요; polite verb 합니다/해요 OR existence 있습니다), 2 scenes (greeting/self-intro, simple exchange), ko-checkpoint-1, ko-unit-1. tokens[] precomputed via your KO tokenizer.
4. KO AUDIO: extend the audio pipeline for Korean (npm run audio:ko, edge-tts ko-KR voice e.g. ko-KR-SunHiNeural), give every KO item an audioId, generate public/audio/ko/*.mp3.
5. Make Korean a REAL picker route (remove the "Soon"/disabled state for KO).

CORRECTNESS > QUANTITY (hard rule): Korean particles, honorific/politeness register, spacing, and hangul must be CORRECT and natural, and STRICTLY closed-universe (linter 0 leaks). Korean is error-prone — USE YOUR CLAUDE HELPER (SIMPLE invocation: prompt to temp file → node bin/peer.mjs ask - < file from tandem dir) to independently cross-check: are the sentences natural? particles right (은/는 vs 이/가, 을/를)? politeness consistent? hangul correct? decodable from only this unit? Do not tell it your answer first.

VERIFY before reporting: npm run lint:content 0 leaks (KO + FR + JA), npm test green (incl. new KO tokenizer fixtures), npm run build green, KO audio generated. Run the ui-craft battery on the Korean picker→learn→scene→checkpoint path (save to .uicraft-shots/iter11-final, give me the path). Don't regress JA/FR.

DO NOT git commit (Watcher commits + redeploys). REPORT: the KO tokenizer design + how it stays fail-closed/exact on the closed lexicon, KO content counts, helper verdict on the Korean (converge/diverge + what it caught), audio result, battery results + path, exact lint/test/build output, and suggested single next step for iter12 (likely: deepen each language toward more units, or final polish + portfolio README).